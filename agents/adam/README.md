# Agent Adam

**Kind:** `orchestrator` (Phase 12)

Default Verse orchestrator plugin. Uses **Kernel only** (`@at72-verse/contracts` + `@at72-verse/verse-kernel`).

Phase 12: deterministic `handleTask()` — fixed JSON plan, no LLM.
