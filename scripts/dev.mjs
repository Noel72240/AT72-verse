/**
 * Phase 01 — no-op workspace orchestrator.
 * Real app/runtime processes start in later phases.
 */
console.log(
  "[at72-verse] Phase 01 bootstrap: no long-running processes yet. Use `pnpm lint` / `pnpm typecheck`.",
);
process.exit(0);
