/**
 * scaffold-skill.ts — reserved CLI entry for monorepo scaffolding.
 *
 * Implementation is scheduled by docs/ROADMAP.md.
 * This module fails loudly until that phase is implemented.
 */
export function main(): never {
  throw new Error(
    "scaffold-skill is not implemented yet. See docs/ROADMAP.md for the scheduled phase.",
  );
}
