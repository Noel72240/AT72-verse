# Railway — Nest API (monorepo)
# Use Dockerfile CMD only — do not set startCommand (it was crashing the service).
[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile.api"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
