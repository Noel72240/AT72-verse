# Railway — Nest API (monorepo)
# Migrations: run locally once against Neon (`pnpm db:migrate`) — see deploy-railway.md
[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile.api"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
