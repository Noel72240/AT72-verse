# Railway — Nest API (monorepo)
# Do not set startCommand — use Dockerfile CMD (migrate + start).
[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile.api"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
